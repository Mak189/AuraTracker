version: "3.9"

services:
  web:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - .:/app  # Mount current directory for live code updates
    environment:
      - FLASK_ENV=development

